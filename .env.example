# URL base de la API. Apunta a la plataforma de administración (PROJECTO DELIVERY)
# donde se gestionan los repartidores y pedidos. Si se sirve tras un proxy que
# une ambos orígenes, puede dejarse vacío para usar el mismo origen.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001